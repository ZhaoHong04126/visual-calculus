const fs=require('fs');const path=require('path');const katex=require('katex');function checkPath(dir){const files=fs.readdirSync(dir);for(const f of files){const fp=path.join(dir,f);if(fs.statSync(fp).isDirectory())checkPath(fp);else if(fp.endsWith('.jsx')){const content=fs.readFileSync(fp,'utf8');const regex=/math=[\"\'](.*?)[\"\']/g;let match;while((match=regex.exec(content))!==null){try{katex.renderToString(match[1],{strict:(err,warn)=>{if(warn.includes('\\c')||warn.includes('accent'))console.log(fp,'====',match[1]);}});}catch(e){}}}}}checkPath('src');
